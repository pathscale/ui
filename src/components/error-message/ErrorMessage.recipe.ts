import { recipe } from "../../lib/layouts";
export const CLASSES = {
  base: "error-message",
} as const;
export const componentRecipe = recipe({component:"error-message",slots:{"error-message":{},"root":{},},});
