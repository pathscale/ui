import { recipe } from "../../lib/layouts";
export const CLASSES = {
  base: "field-error",
} as const;
export const componentRecipe = recipe({component:"field-error",slots:{"field-error":{},"root":{},},});
