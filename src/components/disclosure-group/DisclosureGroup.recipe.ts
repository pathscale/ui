import { recipe } from "../../lib/layouts";
export const CLASSES = {
  base: "disclosure-group",
} as const;
export const componentRecipe = recipe({component:"disclosure-group",slots:{"disclosure-group":{},"root":{},},});
