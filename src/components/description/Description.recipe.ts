import { recipe } from "../../lib/layouts";
export const CLASSES = {
  base: "description",
} as const;
export const componentRecipe = recipe({component:"description",slots:{"description":{},"root":{},},});
