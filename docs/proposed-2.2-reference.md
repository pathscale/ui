# UI/ — built by design & utility

**The proposed 2.2 interface for every component.** *v3 — Composer, Address and Status added; Button's last three `is*` props removed.*

Each entry is the component's *real current* props with the 2.2 rules applied, so nothing here is invented and nothing is missing. Generated by `scripts/generate-proposed-reference.ts`, which is also the executable spec for the codemod.

**73 components** on the main surface · **24** in `@pathscale/ui/lab`

---

## The three axes

| Axis | Answers | Values | |
| --- | --- | --- | --- |
| `variant` | what shape | `solid` `soft` `outline` `ghost` `plain` | closed |
| `flavor` | what it *is* | `neutral` `primary` `secondary` `accent` `destructive` `success` `warning` `info` … | **open** |
| `state` | what is happening *now* | `default` `loading` `error` `invalid` `disabled` `hidden` | closed |

```tsx
<Button>                                       // solid, primary — the call to action
<Button variant="ghost" width="square">        // chrome, icon-only
<Button flavor="destructive" state="loading">  // both, no conflict
<Button flavor="hip">                          // theme-defined, no library change
```

### State, and what an invalid input is

```ts
type State = "default" | "loading" | "error" | "invalid" | "disabled" | "hidden";
```

**An input with invalid input has `state="invalid"`**, and it is readable from `data-state`. One prop, one answer.

`invalid` is *derived by default and settable when you need it* — the same controlled/uncontrolled shape as `value`. Leave `state` alone and the field goes invalid because it has error-severity issues; set it and yours wins.

| `state` set to | Issues | `data-state` | |
| --- | --- | --- | --- |
| — | none | `default` | |
| — | error | **`invalid`** | derived |
| — | warning only | `default` | a warning does not redden the field |
| `disabled` | error | `disabled` | you cannot fix what you cannot edit — but the message still shows |
| `loading` | error | `loading` | revalidating, previous error still visible |
| `hidden` | error | `hidden` | |

An explicit `state` always wins, because a caller who says `disabled` means it. The states otherwise read as a priority: `hidden` over `disabled` over `loading` over `error` over `invalid` over `default`.

### `error` is not `invalid`

| | `invalid` | `error` |
| --- | --- | --- |
| Means | the value broke a rule | something we do not understand went wrong |
| Whose fault | the user's | ours |
| Fixable by editing | yes | no |
| Carries | `Issue[]` — a code and params | a thrown thing |
| Event | `onChange` | **`onError(error, { retry })`** |

The case that forces the distinction is **async validation that cannot reach the server**. The value is not invalid — its validity is *unknown*. Rendering it red and saying "too short" is a lie, and the user will keep editing a field that was fine.

`error` beats `invalid` in the chain for the same reason: if the validator could not run, whatever `issues` still holds is stale and must not be trusted.

Any component doing async work — `Select` loading options, `ChatThread` streaming, `AuthFlow` submitting — extends `Failable` and gets `onError`. Retry belongs there too, because retrying is the response to an exception and is meaningless for a validation issue.

`state` is closed and the library owns it — which conditions a component can be in is not a styling question. `flavor` is the open one.

### Status and connection

Nothing in the library today. Four apps hand-write `ConnectionStatus`, and agencyzero writes three variants of the dot alone (`StatusDot`, `AgentStateDot`, `ItemMarker`). Two levels, both missing.

```ts
// The primitive. daisyUI calls this Status.
type StatusProps = {
  status: string;            // open, like flavor — mirrored to data-status
  label?: JSX.Element;       // from i18n context when omitted
  showLabel?: boolean;
  size?: Size;
  flavor?: Flavor;
  pulse?: boolean;           // activity, e.g. connecting
};

// The complex component.
type ConnectionStatusProps = Failable & {
  connection: "connecting" | "connected" | "disconnected" | "error";
  service?: string;
  url?: string;
  diagnostics?: DiagnosticRow[];
  onReconnect?: () => void | Promise<void>;
  showDiagnostics?: boolean;
};
```

`status` is **open**, for the same reason `flavor` is: agencyzero alone has three unrelated domains — agent state, project status, tab status — and a closed union would have served none of them. It is mirrored to `data-status` so a theme or an app styles its own values.

**`connection` is not `state`.** This is the distinction that matters here and it generalises. `state` is what is happening to *this component*; `connection` is what is happening to the *thing it reports on*. A `ConnectionStatus` can be `state="loading"` while it fetches diagnostics **and** `connection="disconnected"` at the same time — the component is busy, the connection is down, and those are different facts.

The same split applies to every component whose purpose is to display someone else's condition: `Progress` has `value`, `Empty` has its emptiness, `ConnectionStatus` has `connection`. None of them put the subject on `state`.

`onReconnect` and `Failable`'s `onError(error, { retry })` are deliberately separate: reconnecting is a user action on the subject, retrying is a response to the component's own failure.

Not coupled to `@pathscale/wss-adapter`. The apps that use it pass the state in; the library does not learn a transport.

### Combining many conditions into one indicator

A realtime call has at least seven independent conditions:

```
internet --> server --> auth --> media --> peer mic
        |                             +--> peer camera
        +--> your mic
        +--> your camera
```

**"Show the worst one" is the wrong answer.** If the internet is down then six of the seven are also down, and reporting "their camera is off" is true, useless, and sends the user to fix the wrong thing. They are a dependency graph, and the useful report is the **root cause**: the deepest failure whose own dependencies are healthy.

```ts
type Health =
  | "ok" | "degraded" | "flapping" | "connecting" | "reconnecting" | "down" | "unknown";
//  down > reconnecting > connecting > flapping > degraded > unknown > ok

type Quality = "good" | "fair" | "poor" | "unknown";   // orthogonal to health

interface StatusItem {
  id: string; health: Health; quality?: Quality;
  scope?: "local" | "remote";   // only local is actionable by this user
  dependsOn?: string[];
  recoverable?: boolean;         // ICE disconnected (may return) vs failed (will not)
  transitioning?: boolean;       // trying right now
  everHealthy?: boolean;         // "was I ever connected?"
  recentChanges?: number;        // flips — flapping is derived from this
  label?: JSX.Element; detail?: JSX.Element; onRetry?: () => void;
}

summarizeStatus(items) -> { health, quality, attempting?, flapping,
                            recovering, cause?, failing[], symptoms[] }
diffStatus(prev, next, items) -> { degraded[], recovered[], healthChanged, qualityChanged }
```

#### Why each value earns its place

| Value | Why not just `down` |
| --- | --- |
| `connecting` | never worked — be patient |
| `reconnecting` | worked and just broke — **"was I ever connected?"** |
| `flapping` | up *right now*, and that means nothing. Commonly overlooked, which is why a chip reads "Connected" while the experience is unusable |
| `degraded` | works, impaired |
| `unknown` | genuinely cannot tell |

`quality` is a **separate axis** because a call can be `connected` *and* critical at once — folding them forces a choice when both are true.

#### Measured

| Scenario | health | quality | cause | suppressed |
| --- | --- | --- | --- | ---: |
| internet down, all cascades | `down` | good | **Internet** | 6 |
| connected, not authenticated | `degraded` | good | **Signed in** | 2 |
| connected, quality critical | `ok` | **poor** | — | 0 |
| first ever connect | **`connecting`** | good | Server | 0 |
| was up, now retrying | **`reconnecting`** | good | Server | 0 |
| up but flapped 4x | **`flapping`** | **poor** | — | 0 |
| flapping internet, server+media down | `flapping` | poor | **Internet (flapping)** | 2 |
| server reconnecting, media down | **`reconnecting`** | good | **Server** | 1 |

That last row is the rule: **`health` is the worst among *causes*, not symptoms.** Media is down because the server is reconnecting, so reporting `down` would bury the fact that something is actively recovering.

#### State, event, transient

| | | Example |
| --- | --- | --- |
| **State** | a condition you are *in* | `connected` |
| **Event** | a transition, at an instant | "became connected" |
| **Transient** | an event rendered as a state, then gone | `quality-restored` |

Transitions are **emitted**, not just painted, because reconnecting triggers work elsewhere: refetch stale data, resubscribe channels, flush queued writes, re-authenticate. `diffStatus` says which items moved and in which direction, so no consumer has to keep the previous summary and diff it.

`diffStatus` holds no timer — a timer inside a pure function is how a status lies after a tab has been backgrounded for an hour.

## Validation

Every component a user can type into, pick from or toggle extends `Validatable<T>`. **19 components** carry it.

### A result, not a message

```ts
interface Issue {
  code: string;                      // "too_small" — stable and translatable
  params?: Record<string, unknown>;  // { minimum: 8 }
  message?: JSX.Element;             // optional override; skips the i18n lookup
  severity?: "error" | "warning";    // not every issue blocks submission
  path?: (string | number)[];
}
```

`"password too short"` is `{ code: "too_small", params: { minimum: 8 } }`, and the sentence is produced from i18n context. A message baked in English inside the library — which is what `passwordRules.ts` does today — means every non-English app either shows English or rebuilds the result.

### The hard part: showing it *before* the server

Two things are being asked for at once, and conflating them is why inline validation usually feels bad:

| | `Constraint` | `Issue` |
| --- | --- | --- |
| Says | "at least 8 characters" | "too short" |
| Tone | neutral, then satisfied | negative |
| Shown | **live, from the first keystroke** | only once earned |
| Source | the rules, known up front | a failed check |

They are the *same rule at different moments*. Ticking a requirement off as the user types is help. The same rule flashed as "too short" mid-word is nagging. So they are separate props with separate timing.

### Timing

```ts
type ValidateOn = "change" | "blur" | "touched" | "submit";   // default: "touched"
```

`touched` is the only default that is not hostile: **say nothing while they are first typing, surface issues when they leave the field, then revalidate on every keystroke so they can watch themselves fix it.** Reward early, punish late.

| | Behaviour | |
| --- | --- | --- |
| `change` | every keystroke from the start | nags; rarely right |
| `blur` | only on leaving the field | misses the correction feedback |
| `touched` | blur, then live once it has errored once | **the default** |
| `submit` | only on form submit | server-round-trip feel |

### Client and server land in the same place

`issues` is controlled, so a server response sets it directly and it merges with whatever `validate` produced:

```tsx
const [serverIssues, setServerIssues] = createSignal<Issue[]>([]);

<Input
  name="email"
  validate={(v) => v.includes("@") ? [] : [{ code: "invalid_email" }]}
  issues={serverIssues()}          // "that address is already registered"
/>
```

There is no separate error banner and no second rendering path: a server issue looks exactly like a client one, in the same slot, translated the same way.

### Passwords, end to end

The case that motivates the split. Requirements tick live and positive; the failure only appears once earned:

```tsx
<PasswordField
  value={password()}
  onChange={setPassword}
  constraints={evaluatePasswordRules(password(), { minLength: 8, requireNumber: true })}
  showConstraints="unsatisfied"   // fade each one out as it is met
  validateOn="touched"
  issues={serverIssues()}         // "this password has appeared in a breach"
/>
```

`evaluatePasswordRules` already returns `{ key, message, passed }` — it becomes `Constraint[]` by renaming `key` to `code`, `passed` to `satisfied`, and dropping the hardcoded English so i18n supplies the sentence.

`showConstraints`: `always` · `focus` · `unsatisfied` (default for passwords) · `never`.

### It is one label

Both render into the same `message` slot beneath the field, so nothing shifts as validation state changes:

```
┌─────────────────────────────┐
│ Password                    │
│ ┌─────────────────────────┐ │
│ │ ••••••              👁  │ │
│ └─────────────────────────┘ │
│ ✓ At least 8 characters     │   <- Constraint, live
│ ○ One number                │   <- Constraint, unmet
│ ⚠ Appeared in a breach      │   <- Issue, severity warning
└─────────────────────────────┘
```

The slot is `data-slot="field-message"`, and `state="invalid"` is set only when an `error`-severity issue is visible — so a warning colours the message without blocking submission or marking the field red.

---

## Events

22 distinct `on*` props today. They collapse to three, and each carries **why**, not just what.

```ts
onChange?:          (value: T, reason?: ChangeReason) => void;
onOpenChange?:      (open: boolean, reason?: OpenChangeReason) => void;
onSelectionChange?: (keys: K[], reason?: ChangeReason) => void;

type OpenChangeReason = "escape" | "backdrop" | "trigger" | "api" | "select" | "submit";
type ChangeReason     = "input" | "paste" | "clear" | "step" | "select" | "api";
```

The reason is optional and trailing, so `onOpenChange={(open) => …}` is unchanged.

**It exists because the library already drew this distinction.** `Drawer` ships `DrawerCloseReason = "escape" | "backdrop" | "trigger" | "api"`, and folding `onClose` into `onOpenChange(boolean)` would have thrown it away. Whether a dialog closed by Escape or by a backdrop click is exactly what decides whether to warn about unsaved work.

| Kept | Absorbs |
| --- | --- |
| `onChange` | `onInput` (12 sites), `onValueChange` (3), `onToggle` (3), `onSizeChange` (3) |
| `onOpenChange` | `onClose` (24), `onOpen` (3) |
| `onSelectionChange` | `onSelect` (3), `onDaySelect` (6) |

`onAction` (9) and `onInteractOutside` (9) stay distinct: activating an item is not changing a selection, and interacting outside is not necessarily closing.

---

## New in 2.2

Three components that the fleet was rebuilding by hand. Each was measured across `~/code` before it was written, and each is here because something already needed it rather than because the shelf looked incomplete.

| | Evidence | What it carries |
| --- | --- | --- |
| **`Composer`** | one built and tested implementation; 8 repos hand-rolling Enter handling; chat planned on all 12 sites | send on Enter and newline on Shift+Enter *without* breaking IME composition, autosize with a ceiling, refusal to send whitespace or to send twice, and a height announcement for the scroller above it |
| **`Address`** | wallet addresses in 2 apps, and a `truncateAddress` living in one of them and nowhere else | middle truncation, copy with confirmation, explorer link, resolved name over hex |
| **`Status`** | 7 independent conditions in one call, 12 hand-written branches deciding what to show | root-cause aggregation, `Health` including `flapping` and `reconnecting`, quality as its own axis, and transitions as events |

**`Composer` is not `Textarea` with a button.** The reason it is a component is the part nobody writes twice correctly: `isComposing`. While an IME is open, Enter commits the candidate and must not also send the message, and skipping that check sends half a word in Japanese, Chinese and Korean.

**`Address` means the on-chain one.** A postal address form has zero call sites anywhere in the fleet, so building one would be building on a guess.

### What is deliberately *not* a component

`EmailInput`, `UrlInput`, `PhoneInput`. `type="email"` appears in 5 repos and nobody has ever wrapped it, which matches shadcn, daisyUI and Radix — all of which leave it as `<Input type="email">`. What was actually missing is the validation, and 2.2 already has the slot for it: **`type` drives a default `validate` and the `Issue` codes that go with it** — `invalid_email`, `invalid_url`, `invalid_tel` — stable, translatable, and still overridable by passing your own `validate`.

### One place we do not take the industry's word

shadcn spells icon-only `size="icon"`. We spell it **`width="square"`**, because `size="icon"` collapses two axes into one and the fleet needs both: most of the 465 button call sites pass a size *and* icon-only, so `size="icon"` would discard the scale at every one of them. Our own stylesheet had already conceded the point, carrying a separate square width for each size. Taking the industry's term is a tie-breaker on naming, not a reason to lose information.

---

## Renames

| Now | 2.2 |
| --- | --- |
| `Callout` | **`Alert`** |
| `Toggle` | **`Switch`** |
| `Modal` | **`Dialog`** |
| `TextArea` | **`Textarea`** |
| `Disclosure` | **`Collapsible`** |
| `EmptyState` | **`Empty`** |
| `ProgressBar` | **`Progress`** |
| `ProgressCircle` | **`RadialProgress`** |
| `ScrollShadow` | **`ScrollArea`** |
| `Breadcrumbs` | **`Breadcrumb`** |
| `FloatingDock` | **`Dock`** |

---

## Components

### Accordion

Parts: `AccordionContent` · `AccordionIndicator` · `AccordionItem` · `AccordionTrigger`

```ts
type AccordionProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  children?: JSX.Element;
  selectionMode?: AccordionSelectionMode;
  value?: AccordionValue;
  defaultValue?: AccordionValue;
  onValueChange?: (value: string[]) => void;
  hideSeparator?: boolean;
  variant?: Variant;
  state?: State;

};
```

*Changed:* base props renamed; variant → the shared union; isDisabled → state; folded into state.

### Address

```ts
type AddressProps = Omit<JSX.HTMLAttributes<HTMLSpanElement>, "onCopy"> & UIBaseProps & {
  /** The address itself. Always the full value, whatever is displayed. */
  value: string;
  /**
   * A resolved name, shown in place of the hex.
   *
   * ENS and its equivalents are what the address *means* to a person, so when
   * one is known it leads. The hex stays reachable through the title and the
   * copy, because the name is a lookup that can be wrong and the address
   * cannot.
   */
  name?: JSX.Element;
  /** Full URL to a block explorer. No link is rendered without one. */
  explorerUrl?: string;
  truncate?: AddressTruncate;
  /** Characters kept at each end when truncating. */
  lead?: number;
  tail?: number;
  size?: Size;
  state?: State;
  font?: "mono" | "inherit";
  /** Copying is on by default: it is the reason most of these are on screen. */
  copyable?: boolean;
  /** Fired after a successful copy, with the full value. */
  onCopy?: (value: string) => void;
  copyLabel?: JSX.Element;
  copiedLabel?: JSX.Element;
  explorerLabel?: JSX.Element;
};
```

*Changed:* size → the shared scale.

### Alert  ·  was `Callout`

```ts
type AlertProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children" | "title"> & UIBaseProps & {
  flavor?: Flavor;
  variant?: Variant;
  placement?: CalloutPlacement;
  title?: JSX.Element;
  /** `false` suppresses the state's default icon. */
  icon?: JSX.Element | false;
  onDismiss?: () => void;
  dismissLabel?: string;
  children: JSX.Element;
};
```

*Changed:* variant → the shared union.

### AuthCard

```ts
type AuthCardProps = UIBaseProps & {
  title?: JSX.Element;
  description?: JSX.Element;
  children: JSX.Element;
  footer?: JSX.Element;
  brandingSlot?: JSX.Element;
};
```

*Changed:* base props renamed.

### AuthFieldGroup

```ts
type AuthFieldGroupProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  children: JSX.Element;
  gap?: AuthFieldGroupGap;
};
```

*Changed:* base props renamed.

### AuthFooterLinks

```ts
type AuthFooterLinksProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  items: AuthFooterLinkItem[];
  align?: AuthFooterLinksAlign;
};
```

*Changed:* base props renamed.

### AuthMessage

```ts
type AuthMessageProps = UIBaseProps & {
  message?: JSX.Element | string | null;
  flavor?: Flavor;
};
```

### AuthPoweredBy

```ts
type AuthPoweredByProps = UIBaseProps & {
  label?: string;
  logo?: JSX.Element;
  href?: string;
  align?: AuthPoweredByAlign;
  variant?: Variant;
};
```

*Changed:* base props renamed; variant → the shared union.

### AuthSubmitButton

```ts
type AuthSubmitButtonProps = Omit<JSX.ButtonHTMLAttributes<HTMLButtonElement>, "children" | "disabled" | "type"> & UIBaseProps & {
  children: JSX.Element;
  type?: "button" | "submit" | "reset";
  variant?: Variant;
  flavor?: Flavor;
  state?: State;
  size?: Size;
  width?: Width;
};
```

*Changed:* base props renamed; variant → the shared union; size → the shared scale.

### Avatar

Parts: `AvatarFallback` · `AvatarImage`

```ts
type AvatarProps = Omit<JSX.HTMLAttributes<HTMLSpanElement>, "children"> & UIBaseProps & {
  children: JSX.Element;
  size?: Size;
  flavor?: Flavor;
  variant?: Variant;
};
```

*Changed:* base props renamed; color/tone → flavor; variant → the shared union; size → the shared scale.

### Badge

_No exported props type._

### Breadcrumb  ·  was `Breadcrumbs`

Parts: `BreadcrumbsItem`

```ts
type BreadcrumbProps = Omit<JSX.HTMLAttributes<HTMLElement>, "children"> & UIBaseProps & {
  children: JSX.Element;
  separator?: JSX.Element;
};
```

*Changed:* base props renamed.

### Button

Parts: `ButtonGroup` · `ButtonGroupSeparator`

```ts
type ButtonProps = Omit<JSX.ButtonHTMLAttributes<HTMLButtonElement>, "color" | "type"> & UIBaseProps & IconSlotProps & {
  variant?: Variant;
  flavor?: Flavor;
  state?: State;
  size?: Size;
  /** `square` is icon-only: as wide as it is tall, at whatever size it is. */
  width?: Width | "square";
  radius?: Radius;
  type?: "button" | "submit" | "reset";
  children?: JSX.Element;
};
```

*Changed:* variant → the shared union; size → the shared scale.

### Calendar

```ts
type CalendarProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "onChange" | "children"> & UIBaseProps & CalendarBaseProps;
```

*Changed:* base props renamed.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### Card

Parts: `CardBody` · `CardFooter` · `CardHeader` · `CardRoot`

```ts
type CardProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  variant?: Variant;
  material?: CardMaterial;
  elevation?: CardElevation;
  flavor?: Flavor;
  state?: State;
  padding?: Space;
  radius?: Radius;
  /** Replaces isHoverable and isPressable, which had one call site each across 330. */
  isInteractive?: boolean;
  header?: JSX.Element;
  footer?: JSX.Element;
  children: JSX.Element;
};
```

*Changed:* variant → the shared union.

### ChatBubble

```ts
type ChatBubbleProps = UIBaseProps & JSX.HTMLAttributes<HTMLDivElement> & {
  end?: boolean;
};
```

*Changed:* base props renamed.

### Checkbox

Parts: `CheckboxGroup`

```ts
type CheckboxProps = Omit<JSX.InputHTMLAttributes<HTMLInputElement>, "type" | "children"> & UIBaseProps & {
  children?: JSX.Element;
  description?: JSX.Element;
  state?: State;

  isIndeterminate?: boolean;
  indeterminate?: boolean;
  variant?: Variant;
};
```

*Changed:* base props renamed; variant → the shared union; isDisabled → state; folded into state; folded into defaultValue.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### Chip

_No exported props type._

### Collapsible  ·  was `Disclosure`

```ts
type CollapsibleProps = ComponentProps<typeof DisclosureRoot>;
```

### ColorSwatch

Parts: `ColorSwatchPicker`

```ts
type ColorSwatchProps = Omit<JSX.ButtonHTMLAttributes<HTMLButtonElement>, "onChange" | "onSelect"> & UIBaseProps & {
  color: string;
  colorName?: string;
  shape?: ColorSwatchShape;
  size?: Size;
  isSelected?: boolean;
  state?: State;
  onSelect?: (color: string) => void;
  onChange?: (color: string) => void;
};
```

*Changed:* base props renamed; size → the shared scale; isDisabled → state.

### Composer

```ts
type ComposerProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "onChange" | "onSubmit" | "children"> & UIBaseProps & {
  value?: string;
  defaultValue?: string;
  onChange?: (value: string, reason?: ChangeReason) => void;
  /** Fired with the trimmed message. Not fired while `state` is `loading`. */
  onSubmit?: (value: string) => void;
  placeholder?: string;
  size?: Size;
  variant?: Variant;
  radius?: Radius;
  /** `loading` means a send is in flight: the box stays readable and refuses to send again. */
  state?: State;
  /** Server-side or asynchronous problems, rendered under the box. */
  issues?: Issue[];
  minRows?: number;
  maxRows?: number;
  /**
   * Enter sends. Set false where a newline is the common case and sending is
   * an explicit act, which is what long-form editors want.
   */
  submitOnEnter?: boolean;
  /**
   * Announced whenever the box changes height.
   *
   * A chat transcript that pins to the bottom has to know: the composer
   * growing by a line moves the bottom, and a scroller that finds out by
   * observing the DOM finds out a frame late and jitters.
   */
  onHeightChange?: (height: number) => void;
  /** Controls before the field: attachments, model pickers, mode switches. */
  lead?: JSX.Element;
  /** Controls after the field, before the submit button. */
  trail?: JSX.Element;
  submitLabel?: JSX.Element;
  /** Shown beside the controls. Usually the Enter/Shift+Enter reminder. */
  hint?: JSX.Element;
  autofocus?: boolean;
  name?: string;
  /** Height of the window, for the ceiling. Injected for runtimes without one. */
  viewportHeight?: number;
};
```

*Changed:* variant → the shared union; size → the shared scale.

### CookieConsent

_No exported props type._

### Dialog  ·  was `Modal`

Parts: `ModalBackdrop` · `ModalBody` · `ModalCloseTrigger` · `ModalContent` · `ModalFooter` · `ModalHeader` · `ModalHeading` · `ModalIcon` · `ModalTrigger`

```ts
type DialogProps = ModalRootProps;
```

### Dock  ·  was `FloatingDock`

```ts
type DockProps = {
  items: FloatingDockItem[];
  orientation?: "horizontal" | "vertical";
  tooltipDirection?: FloatingDockDirection;
  mobilePopupDirection?: FloatingDockDirection;
  /** How to render on mobile: "burger" shows a toggle popup, "dock" shows the full dock bar. @default "burger" */
  mobileMode?: "burger" | "dock";
  gap?: number;
  baseSize?: number;
  hoverSize?: number;
  iconSize?: number;
  hoverIconSize?: number;
  magnifyRange?: number;
  magnify?: boolean;
  nudge?: number;
  showDesktop?: boolean;
  showMobile?: boolean;
  showContainer?: boolean;
  desktopClass?: string;
  mobileClass?: string;
  itemClass?: string;
  tooltipClass?: string;
  mobileToggleIcon?: JSX.Element;
  springMass?: number;
  springStiffness?: number;
  springDamping?: number;
} & UIBaseProps;
```

*Changed:* base props renamed.

### Drawer

Parts: `DrawerBackdrop` · `DrawerBody` · `DrawerClose` · `DrawerCloseTrigger` · `DrawerContent` · `DrawerDialog` · `DrawerFooter` · `DrawerHandle` · `DrawerHeader` · `DrawerHeading` · `DrawerTrigger`

```ts
type DrawerProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  children: JSX.Element;
  open?: boolean;
  defaultOpen?: boolean;
  onOpenChange?: (open: boolean) => void;
  defaultOpen?: boolean;
  onOpenChange?: (isOpen: boolean) => void;
  placement?: DrawerPlacement;
  size?: Size;
  backdrop?: DrawerBackdropVariant;
  scrollBehavior?: DrawerScrollBehavior;
  isDismissable?: boolean;
  shouldCloseOnEsc?: boolean;
  shouldCloseOnBackdropClick?: boolean;
  trapFocus?: boolean;
  restoreFocus?: boolean;
};
```

*Changed:* base props renamed; size → the shared scale; controlled triple.

### Dropdown

_No exported props type._

### Empty  ·  was `EmptyState`

```ts
type EmptyProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  children: JSX.Element;
};
```

*Changed:* base props renamed.

### FieldGroup

```ts
type FieldGroupProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  children?: JSX.Element;
};
```

*Changed:* base props renamed.

### Fieldset

Parts: `FieldsetActions` · `FieldsetLegend`

```ts
type FieldsetProps = JSX.FieldsetHTMLAttributes<HTMLFieldSetElement> & UIBaseProps;
```

*Changed:* base props renamed.

### FirefoxPWABanner

_No exported props type._

### Flex

```ts
type FlexProps = UIBaseProps & Omit<JSX.HTMLAttributes<HTMLElement>, "ref"> & {
  as?: keyof JSX.IntrinsicElements;
  direction?: ResponsiveProp<"row" | "col" | "row-reverse" | "col-reverse">;
  justify?: ResponsiveProp<"start" | "center" | "end" | "between" | "around" | "evenly">;
  align?: ResponsiveProp<"start" | "center" | "end" | "stretch" | "baseline">;
  wrap?: ResponsiveProp<"wrap" | "nowrap" | "wrap-reverse">;
  gap?: ResponsiveProp<"none" | "sm" | "md" | "lg" | "xl">;
  gapX?: ResponsiveProp<"none" | "sm" | "md" | "lg" | "xl">;
  gapY?: ResponsiveProp<"none" | "sm" | "md" | "lg" | "xl">;
  paddingInline?: ResponsiveProp<"none" | "sm" | "md" | "lg" | "xl">;
  paddingBlock?: ResponsiveProp<"none" | "sm" | "md" | "lg" | "xl">;
  width?: ResponsiveProp<"full">;
  height?: ResponsiveProp<"full">;
  minWidth?: ResponsiveProp<"zero">;
  minHeight?: ResponsiveProp<"zero">;
  grow?: ResponsiveProp<boolean>;
  shrink?: ResponsiveProp<boolean>;
  basis?: ResponsiveProp<"none" | "sm" | "md" | "lg" | "xl">;
};
```

*Changed:* base props renamed.

### Footer

```ts
type FooterProps = UIBaseProps & {
  children?: JSX.Element;
  center?: boolean;
  horizontal?: boolean;
  vertical?: boolean;
};
```

*Changed:* base props renamed.

### Form

Parts: `FormContext` · `FormField` · `FormSubmitButton` · `FormWithContext`

```ts
type FormProps = JSX.FormHTMLAttributes<HTMLFormElement> & UIBaseProps;
```

*Changed:* base props renamed.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### GlowCard

```ts
type GlowCardProps = UIBaseProps & JSX.HTMLAttributes<HTMLDivElement>;
```

*Changed:* base props renamed.

### Grid

```ts
type GridProps = UIBaseProps & Omit<JSX.HTMLAttributes<HTMLElement>, "ref"> & {
  as?: keyof JSX.IntrinsicElements;
  cols?: ResponsiveProp<GridSize>;
  rows?: ResponsiveProp<GridSize>;
  flow?: ResponsiveProp<GridFlow>;
  gap?: ResponsiveProp<GridGap>;
  autoCols?: ResponsiveProp<AutoSize>;
  autoRows?: ResponsiveProp<AutoSize>;
};
```

*Changed:* base props renamed.

### Header

```ts
type HeaderProps = JSX.HTMLAttributes<HTMLDivElement> & UIBaseProps;
```

*Changed:* base props renamed.

### I18nContext

_No exported props type._

### I18nProvider

_No exported props type._

### Icon

```ts
type IconProps = UIBaseProps & {
  width?: number;
  height?: number;
  flavor?: Flavor;
  name?: string;
};
```

*Changed:* base props renamed; color/tone → flavor.

### ImmersiveLanding

Parts: `ImmersiveLandingContext`

_No exported props type._

### Input

Parts: `InputOTP` · `InputOTPGroup` · `InputOTPSeparator` · `InputOTPSlot`

_No exported props type._

### InputOTP

Parts: `InputOTPGroup` · `InputOTPSeparator` · `InputOTPSlot`

```ts
type InputOTPProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children" | "onChange" | "onInput"> & UIBaseProps & {
  children?: JSX.Element;
  value?: string;
  defaultValue?: string;
  onChange?: (value: string) => void;
  onComplete?: (value: string) => void;
  maxLength?: number;
  variant?: Variant;
  pattern?: string;
  name?: string;
  autoFocus?: boolean;
  state?: State;

  inputClassName?: string;
  inputMode?: JSX.InputHTMLAttributes<HTMLInputElement>["inputMode"];
};
```

*Changed:* base props renamed; variant → the shared union; isDisabled → state; folded into state.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### Label

```ts
type LabelProps = Omit<JSX.LabelHTMLAttributes<HTMLLabelElement>, "for"> & UIBaseProps & {
  for?: string;
  htmlFor?: string;
  required?: boolean;
  state?: State;

};
```

*Changed:* base props renamed; isDisabled → state; folded into state; native HTML attribute.

### LanguageSwitcher

_No exported props type._

### Link

Parts: `LinkIcon`

```ts
type LinkProps = LinkRootProps;
```

### ListBox

Parts: `ListBoxItem` · `ListBoxItemIndicator` · `ListBoxSection`

```ts
type ListBoxProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  children?: JSX.Element;
  title?: JSX.Element;
};
```

*Changed:* base props renamed.

### LiveChat

Parts: `LiveChatBubble` · `LiveChatPanel`

_No exported props type._

### MetalBorder

```ts
type MetalBorderProps = UIBaseProps & Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & {
  children?: JSX.Element;
  preset?: MetalBorderPreset;
  strength?: number;
  kind?: MetalBorderKind;
  glow?: boolean;
  paused?: boolean;
  theme?: MetalBorderTheme;
  cornerRadius?: number | string;
  contentClass?: string;
};
```

*Changed:* base props renamed.

### Navbar

```ts
type NavbarProps = JSX.HTMLAttributes<HTMLElement> & UIBaseProps & {
  as?: keyof JSX.IntrinsicElements;

};
```

*Changed:* base props renamed; className deleted — 7 sites against 302 for class; carried by UIBaseProps.

### Pagination

```ts
type PaginationProps = Omit<JSX.HTMLAttributes<HTMLElement>, "onChange"> & UIBaseProps & {
  page: number;
  total: number;
  onChange: (page: number) => void;
  state?: State;
};
```

*Changed:* base props renamed; isDisabled → state.

### PasswordField

```ts
type PasswordFieldProps = UIBaseProps & {
  id?: string;
  name?: string;
  label?: JSX.Element;
  placeholder?: string;

  invalid?: boolean;
  required?: boolean;
  autofocus?: boolean;
  autocomplete?: "current-password" | "new-password" | "off";
  "aria-describedby"?: string;
  startIcon?: JSX.Element;
  showLabel: string;
  hideLabel: string;
  value?: string;
  inputRef?: (el: HTMLInputElement) => void;
  onInput?: (value: string) => void;
  onBlur?: () => void;
  visibleIcon?: JSX.Element;
  hiddenIcon?: JSX.Element;
  onVisibilityChange?: (visible: boolean) => void;
  class?: string;
  inputClass?: string;
};
```

*Changed:* base props renamed; folded into state.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### PasswordRequirements

```ts
type PasswordRequirementsProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  title?: JSX.Element;
  results: PasswordRuleResult[];
  metIcon?: JSX.Element;
  unmetIcon?: JSX.Element;
};
```

*Changed:* base props renamed.

### PasswordRules

_No exported props type._

### Popover

```ts
type PopoverProps = ComponentProps<typeof PopoverRoot>;
```

### Progress  ·  was `ProgressBar`

```ts
type ProgressProps = UIBaseProps & Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & {
  value?: number;
  minValue?: number;
  maxValue?: number;
  isIndeterminate?: boolean;
  label?: string;
  size?: Size;
  flavor?: Flavor;
  state?: State;
  formatValue?: (value: number) => string;
  showValue?: boolean;
};
```

*Changed:* base props renamed; color/tone → flavor; size → the shared scale; isDisabled → state.

### PWAInstallPrompt

_No exported props type._

### Radio

Parts: `RadioGroup`

```ts
type RadioProps = Omit<JSX.InputHTMLAttributes<HTMLInputElement>, "type" | "children"> & UIBaseProps & {
  children?: JSX.Element;
  description?: JSX.Element;
  indicator?: JSX.Element;
  state?: State;

};
```

*Changed:* base props renamed; isDisabled → state; folded into state.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### RadioGroup

```ts
type RadioGroupProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children" | "onChange"> & UIBaseProps & {
  children: JSX.Element;
  value?: string;
  defaultValue?: string;
  onChange?: (value: string) => void;
  name?: string;
  state?: State;

  orientation?: RadioGroupOrientation;
  variant?: Variant;
  label?: JSX.Element;
  description?: JSX.Element;
  errorMessage?: JSX.Element;
};
```

*Changed:* base props renamed; variant → the shared union; isDisabled → state; folded into state.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### ScrollArea  ·  was `ScrollShadow`

```ts
type ScrollAreaProps = UIBaseProps & Omit<JSX.HTMLAttributes<HTMLDivElement>, "size"> & {
  size?: number;
  offset?: number;
  visibility?: ScrollShadowVisibility;
  isEnabled?: boolean;
  orientation?: ScrollShadowOrientation;
  variant?: Variant;
  hideScrollBar?: boolean;
  onVisibilityChange?: (visibility: ScrollShadowVisibility) => void;
};
```

*Changed:* base props renamed; variant → the shared union.

### Select

```ts
type SelectProps = SelectRootProps;
```

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### Separator

```ts
type SeparatorProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  orientation?: SeparatorOrientation;
  variant?: Variant;
};
```

*Changed:* base props renamed; variant → the shared union.

### Skeleton

```ts
type SkeletonProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  shape?: SkeletonShape;
  /** Token, or a raw number of pixels for the cases a scale cannot cover. */
  width?: Width | number;
  height?: Size | number;
  size?: Size;
  radius?: Radius;
  animation?: SkeletonAnimation;
  /** Renders a stack of lines for a multi-line text placeholder. */
  lines?: number;
};
```

*Changed:* size → the shared scale.

### Slider

```ts
type SliderProps = SliderBaseProps & UIBaseProps & Omit<JSX.InputHTMLAttributes<HTMLInputElement>, keyof SliderBaseProps>;
```

*Changed:* base props renamed.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### Spinner

```ts
type SpinnerProps = Omit<JSX.HTMLAttributes<HTMLSpanElement>, "children"> & UIBaseProps & {
  size?: Size;
  state?: State;
  shape?: SpinnerShape;
  label?: string;
};
```

*Changed:* size → the shared scale.

### Status

_No exported props type._

### Switch  ·  was `Toggle`

```ts
type SwitchProps = Omit<JSX.InputHTMLAttributes<HTMLInputElement>, "type" | "children" | "color"> & UIBaseProps & {
  children?: JSX.Element;
  description?: JSX.Element;
  icon?: JSX.Element;
  state?: State;
  flavor?: Flavor;
  size?: Size;
};
```

*Changed:* base props renamed; color/tone → flavor; size → the shared scale; isDisabled → state; folded into defaultValue.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### Table

Parts: `TableExpandToggle` · `TableInlineConfirm` · `TableMobileListView` · `TableSortIcon` · `TableVirtualSpacerRow`

```ts
type TableProps = JSX.HTMLAttributes<HTMLDivElement> & UIBaseProps & {
  variant?: Variant;
};
```

*Changed:* base props renamed; variant → the shared union.

### Tabs

_No exported props type._

### Text

Parts: `TextArea`

```ts
type TextProps = Omit<JSX.HTMLAttributes<HTMLSpanElement>, "color"> & UIBaseProps & {
  size?: Size;
  variant?: Variant;
  weight?: TextWeight;
  transform?: TextTransform;
  tracking?: TextTracking;
  leading?: TextLeading;
  family?: TextFamily;
  children?: JSX.Element;
};
```

*Changed:* base props renamed; variant → the shared union; size → the shared scale.

### Textarea  ·  was `TextArea`

```ts
type TextareaProps = Omit<JSX.TextareaHTMLAttributes<HTMLTextAreaElement>, "children"> & UIBaseProps & {
  variant?: Variant;
  width?: Width;
  state?: State;

};
```

*Changed:* base props renamed; variant → the shared union; isDisabled → state; folded into state; → width.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### ThemeColorPicker

_No exported props type._

### Toast

Parts: `ToastActionButton` · `ToastCloseButton` · `ToastContent` · `ToastDescription` · `ToastIndicator` · `ToastProvider` · `ToastQueue` · `ToastTitle`

```ts
type ToastProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children" | "title"> & UIBaseProps & {
  children?: JSX.Element;
  title?: JSX.Element;
  description?: JSX.Element;
  variant?: Variant;
  indicator?: JSX.Element;
  actionProps?: ToastActionProps;

  onClose?: () => void;
  isFrontmost?: boolean;
  isHidden?: boolean;
  isEntering?: boolean;
  isExiting?: boolean;
};
```

*Changed:* base props renamed; variant → the shared union; folded into state.

### Tooltip

Parts: `TooltipArrow` · `TooltipContent` · `TooltipTrigger`

```ts
type TooltipProps = UIBaseProps & {
  children: JSX.Element;
  placement?: TooltipPlacement;
  autoFlip?: boolean;
  sideOffset?: number;
  showArrow?: boolean;
  delay?: number;
  closeDelay?: number;
  open?: boolean;
  defaultOpen?: boolean;
  onOpenChange?: (open: boolean) => void;
  defaultOpen?: boolean;
  onOpenChange?: (isOpen: boolean) => void;
};
```

*Changed:* base props renamed; controlled triple.

---

## `@pathscale/ui/lab`

### ButtonGroup

Parts: `ButtonGroupSeparator`

```ts
type ButtonGroupProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  children?: JSX.Element;
  orientation?: ButtonGroupOrientation;
  size?: Size;
  variant?: Variant;
  state?: State;
  width?: Width;
};
```

*Changed:* base props renamed; variant → the shared union; size → the shared scale; isDisabled → state; → width.

### CheckboxGroup

```ts
type CheckboxGroupProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children" | "onChange"> & UIBaseProps & {
  children?: JSX.Element | ((values: string[]) => JSX.Element);
  value?: string[];
  defaultValue?: string[];
  onChange?: (value: string[]) => void;
  name?: string;
  state?: State;

  variant?: Variant;
};
```

*Changed:* base props renamed; variant → the shared union; isDisabled → state; folded into state.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### CloseButton

```ts
type CloseButtonProps = Omit<JSX.ButtonHTMLAttributes<HTMLButtonElement>, "disabled"> & UIBaseProps & {
  variant?: Variant;
  state?: State;

  startIcon?: JSX.Element;
  endIcon?: JSX.Element;

};
```

*Changed:* base props renamed; className deleted — 7 sites against 302 for class; variant → the shared union; isDisabled → state; folded into state.

### ColorArea

```ts
type ColorAreaProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children" | "onChange"> & UIBaseProps & {
  value?: ColorAreaValue;
  onChange?: (value: ColorAreaValue) => void;
  state?: State;
};
```

*Changed:* base props renamed; isDisabled → state.

### ColorField

```ts
type ColorFieldProps = Omit<JSX.InputHTMLAttributes<HTMLInputElement>, "type" | "value" | "defaultValue" | "onChange"> & UIBaseProps & {
  value?: string;
  defaultValue?: string;
  onChange?: (value: string) => void;
  state?: State;
  format?: ColorFieldFormat;
  width?: Width;
};
```

*Changed:* base props renamed; isDisabled → state; → width.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### ColorPicker

```ts
type ColorPickerProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children" | "onChange"> & UIBaseProps & {
  children?: JSX.Element;
  value?: string;
  defaultValue?: string;
  onChange?: (value: string) => void;
  state?: State;
};
```

*Changed:* base props renamed; isDisabled → state.

### ColorSlider

```ts
type ColorSliderProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children" | "onChange"> & UIBaseProps & {
  value?: number;
  defaultValue?: number;
  onChange?: (value: number) => void;
  type?: ColorSliderType;
  state?: State;
};
```

*Changed:* base props renamed; isDisabled → state.

### ColorSwatchPicker

```ts
type ColorSwatchPickerProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "onChange"> & UIBaseProps & {
  children: JSX.Element;
  value?: string;
  defaultValue?: string;
  onChange?: (value: string) => void;
  state?: State;
};
```

*Changed:* base props renamed; isDisabled → state.

### ColorWheelFlower

_No exported props type._

### ComboBox

Parts: `ComboBoxInput` · `ComboBoxInputGroup` · `ComboBoxList` · `ComboBoxPopover` · `ComboBoxTrigger`

```ts
type ComboBoxProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  children?: JSX.Element;
};
```

*Changed:* base props renamed.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### DateField

Parts: `DateFieldGroup` · `DateFieldInput` · `DateFieldInputContainer` · `DateFieldPrefix` · `DateFieldSegment` · `DateFieldSuffix`

```ts
type DateFieldProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children" | "onChange" | "onBlur"> & UIBaseProps & {
  children?: JSX.Element | ((props: DateFieldRenderProps) => JSX.Element);
  name?: string;
  value?: string;
  defaultValue?: string;
  onChange?: (value: string) => void;
  onBlur?: JSX.EventHandlerUnion<HTMLInputElement, FocusEvent>;
  width?: Width;
  variant?: Variant;
  state?: State;

  required?: boolean;
  required?: boolean;
};
```

*Changed:* base props renamed; variant → the shared union; isDisabled → state; folded into state; native HTML attribute; → width.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### DatePicker

```ts
type DatePickerProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "onChange" | "children"> & UIBaseProps & DatePickerBaseProps;
```

*Changed:* base props renamed.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### DateRangePicker

```ts
type DateRangePickerProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "onChange" | "children"> & UIBaseProps & DateRangePickerBaseProps;
```

*Changed:* base props renamed.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### Join

```ts
type JoinProps = UIBaseProps & JSX.HTMLAttributes<HTMLDivElement> & {
  responsive?: boolean;
  vertical?: boolean;
  horizontal?: boolean;
};
```

*Changed:* base props renamed.

### Kbd

Parts: `KbdAbbr` · `KbdContent`

```ts
type KbdProps = KbdRootProps;
```

### Menu

Parts: `MenuItem` · `MenuItemIndicator` · `MenuItemSubmenuIndicator` · `MenuSection`

```ts
type MenuProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & UIBaseProps & {
  children?: JSX.Element;
  title?: JSX.Element;
};
```

*Changed:* base props renamed.

### Meter

Parts: `MeterFill` · `MeterOutput` · `MeterTrack`

```ts
type MeterProps = UIBaseProps & Omit<JSX.HTMLAttributes<HTMLDivElement>, "children"> & {
  children?: JSX.Element | ((state: MeterRenderState) => JSX.Element);
  value?: number;
  minValue?: number;
  maxValue?: number;
  lowValue?: number;
  highValue?: number;
  optimumValue?: number;
  state?: State;
  size?: Size;
  flavor?: Flavor;
  formatOptions?: Intl.NumberFormatOptions;
  formatValue?: (value: number, state: Omit<MeterRenderState, "valueText">) => string;
};
```

*Changed:* base props renamed; color/tone → flavor; size → the shared scale; isDisabled → state.

### NoiseBackground

```ts
type NoiseBackgroundProps = {
  children?: JSX.Element;
  /** Additional CSS classes for the outer container. */
  containerClass?: string;
  /** Gradient blob colors (provide 2–3). */
  gradientColors?: string[];
  /** Noise texture opacity (0–1). @default 0.2 */
  noiseIntensity?: number;
  /** Animation velocity multiplier. @default 0.1 */
  speed?: number;
  /** Apply a backdrop-blur over the gradient layers. @default false */
  backdropBlur?: boolean;
  /** Enable/disable the wandering animation. @default true */
  animating?: boolean;
  /** CSS border-radius value. @default "var(--radius-box, 1rem)" */
  borderRadius?: string;
  /** Path or URL to the noise texture image. @default "/noise.webp" */
  noiseSrc?: string;
  /** Show the static noise texture overlay. @default false */
  showNoise?: boolean;
} & UIBaseProps;
```

*Changed:* base props renamed.

### RadialProgress  ·  was `ProgressCircle`

```ts
type RadialProgressProps = UIBaseProps & Omit<JSX.HTMLAttributes<HTMLSpanElement>, "children"> & {
  value?: number;
  minValue?: number;
  maxValue?: number;
  isIndeterminate?: boolean;
  size?: Size;
  flavor?: Flavor;
  state?: State;
  formatValue?: (value: number) => string;
  label?: string;
};
```

*Changed:* base props renamed; color/tone → flavor; size → the shared scale; isDisabled → state.

### RangeCalendar

```ts
type RangeCalendarProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "onChange" | "children"> & UIBaseProps & RangeCalendarBaseProps;
```

*Changed:* base props renamed.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### SizePicker

_No exported props type._

### TimeField

Parts: `TimeFieldGroup` · `TimeFieldInput` · `TimeFieldInputContainer` · `TimeFieldPrefix` · `TimeFieldSegment` · `TimeFieldSuffix`

```ts
type TimeFieldProps = Omit<JSX.HTMLAttributes<HTMLDivElement>, "children" | "onChange" | "onBlur"> & UIBaseProps & {
  children?: JSX.Element | ((props: TimeFieldRenderProps) => JSX.Element);
  name?: string;
  value?: string;
  defaultValue?: string;
  onChange?: (value: string) => void;
  onBlur?: JSX.EventHandlerUnion<HTMLInputElement, FocusEvent>;
  width?: Width;
  variant?: Variant;
  state?: State;

  required?: boolean;
  required?: boolean;
};
```

*Changed:* base props renamed; variant → the shared union; isDisabled → state; folded into state; native HTML attribute; → width.

Also extends **`Validatable<T>`** — `validate`, `validateOn`, `issues`, `constraints`, `errorMessage`. See [Validation](#validation).

### Toolbar

```ts
type ToolbarProps = UIBaseProps & JSX.HTMLAttributes<HTMLDivElement> & {
  orientation?: ToolbarOrientation;
  isAttached?: boolean;
};
```

*Changed:* base props renamed.

### VideoPreview

```ts
type VideoPreviewProps = VideoPreviewBaseProps & UIBaseProps & Omit<JSX.VideoHTMLAttributes<HTMLVideoElement>, keyof VideoPreviewBaseProps>;
```

*Changed:* base props renamed.
