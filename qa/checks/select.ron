// Generated from qa/components.ts by qa/generate-checks.ts. Do not edit.
//
// Select, mounted alone on its own harness page. Isolation is
// the point: driving this inside a real application makes every check
// order-dependent, and a failure caused by the previous group's leftover
// state is indistinguishable from a real one.
[
    (
        id: "select-page-paints",
        group: "select",
        what: "the Select page builds and paints",
        open: None,
        hover: None,
        click: None,
        subject: "heading:Select",
        expect: PaintsNamed,
    ),
    (
        id: "select-renders",
        group: "select",
        what: "Select renders a node of its own",
        open: None,
        hover: None,
        click: None,
        subject: "fixture",
        expect: Paints,
    ),
    (
        id: "select-opens",
        group: "select",
        what: "the Select menu reaches the renderer as an addressable item",
        open: None,
        hover: None,
        prepare: Some("Session"),
        settle_after_ms: 600,
        click: None,
        subject: "option:first fixture",
        expect: PaintsNamed,
    ),
    (
        id: "select-changes",
        group: "select",
        what: "choosing another value changes what the Select trigger reads",
        open: None,
        hover: None,
        prepare: Some("Session"),
        settle_after_ms: 600,
        click: Some("option:second fixture"),
        subject: "button:Session",
        expect: NameChanges,
    ),
    (
        id: "select-escape-closes",
        group: "select",
        what: "Escape closes the Select menu without choosing",
        open: None,
        hover: None,
        prepare: Some("Session"),
        settle_after_ms: 600,
        prepare_key: Some("Escape"),
        click: None,
        subject: "option:first fixture",
        expect: Vanishes,
    ),
]
