// Generated from qa/components.ts by qa/generate-checks.ts. Do not edit.
//
// PasswordField, mounted alone on its own harness page. Isolation is
// the point: driving this inside a real application makes every check
// order-dependent, and a failure caused by the previous group's leftover
// state is indistinguishable from a real one.
[
    (
        id: "password-field-page-paints",
        group: "password-field",
        what: "the PasswordField page builds and paints",
        open: None,
        hover: None,
        click: None,
        subject: "heading:PasswordField",
        expect: PaintsNamed,
    ),
    (
        id: "password-field-renders",
        group: "password-field",
        what: "PasswordField renders a node of its own",
        open: None,
        hover: None,
        click: None,
        subject: "fixture",
        expect: Paints,
    ),
    (
        id: "password-field-opens",
        group: "password-field",
        what: "the PasswordField menu reaches the renderer as an addressable item",
        open: None,
        hover: None,
        prepare: Some("PasswordField"),
        settle_after_ms: 600,
        click: None,
        subject: "textbox:PasswordField",
        expect: PaintsNamed,
    ),
    (
        id: "password-field-changes",
        group: "password-field",
        what: "choosing another value changes what the PasswordField trigger reads",
        open: None,
        hover: None,
        prepare: Some("PasswordField"),
        settle_after_ms: 600,
        click: Some("textbox:PasswordField"),
        subject: "textbox:PasswordField",
        expect: NameChanges,
    ),
    (
        id: "password-field-escape-closes",
        group: "password-field",
        what: "Escape closes the PasswordField menu without choosing",
        open: None,
        hover: None,
        prepare: Some("PasswordField"),
        settle_after_ms: 600,
        prepare_key: Some("Escape"),
        click: None,
        subject: "textbox:PasswordField",
        expect: Vanishes,
    ),
]
